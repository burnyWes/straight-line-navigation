{
  "name": "straight-line-navigation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Luftlinie und Richtung zu selbst gespeicherten Orten - ohne Karte, ohne Routing.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "icons": "node tools/make-icons.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
